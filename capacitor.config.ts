import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fh-technikum.publictransportextended',
  appName: 'public-transport-extended_v2',
  webDir: 'dist'
};

export default config;
